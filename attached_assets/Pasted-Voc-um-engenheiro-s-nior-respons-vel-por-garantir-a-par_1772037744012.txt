Você é um engenheiro sênior responsável por garantir a paridade entre desenvolvimento e produção antes de um redeploy no Replit. 

Faça uma auditoria completa do projeto seguindo os blocos abaixo. Para cada item, responda com ✅ OK, ⚠️ ATENÇÃO ou ❌ BLOQUEANTE, e explique brevemente o que encontrou. Ao final, emita um veredicto: APROVADO PARA DEPLOY / REPROVADO — e liste os itens bloqueantes se houver.

---

### BLOCO 1 — SECRETS & VARIÁVEIS DE AMBIENTE

1. Liste todas as variáveis de ambiente referenciadas no código (os.environ, os.getenv, process.env). 
2. Compare com a lista de Secrets cadastrados no painel do Replit (Secrets tab). Identifique qualquer variável usada no código que NÃO está cadastrada como Secret.
3. Verifique se há alguma chave ou credencial hardcoded diretamente no código (strings que parecem API keys, tokens, senhas, connection strings). Se encontrar, isso é ❌ BLOQUEANTE.
4. Confirme que as variáveis críticas do sistema estão presentes: OPENAI_API_KEY (ou equivalente), DATABASE_URL (ou conexão vetorial), WHATSAPP_TOKEN (se aplicável), e qualquer outra chave de integração usada pelo pipeline RAG.

---

### BLOCO 2 — BANCO DE DADOS E VECTOR STORE

5. Verifique se o código referencia corretamente o banco de dados de produção. No Replit, dev e prod têm databases separados — confirme que não há nenhuma string de conexão hardcoded apontando para um ambiente específico que pode divergir.
6. Identifique se houve mudanças no schema do banco (novas tabelas, novas colunas, índices novos) desde o último deploy. Liste as mudanças encontradas.
7. Se houver migrações pendentes (Alembic, migrate, etc.), liste-as. A ausência de migration para uma mudança de schema é ❌ BLOQUEANTE.
8. Verifique se o RetrievalLog (ou tabela equivalente de observabilidade) tem todas as colunas novas esperadas: intent_detected, entities_detected, composite_score_max, web_search_used, blocks_with_scores, is_comparative. Se as colunas existem no código mas não há migration, é ❌ BLOQUEANTE.

---

### BLOCO 3 — PIPELINE RAG — PARIDADE DE FUNÇÕES

9. Confirme que o arquivo agent_test.py usa EnhancedSearch.search (e NÃO vector_store.search diretamente). Se ainda chamar vector_store.search diretamente para a busca principal, é ❌ BLOQUEANTE.
10. Confirme que a função _ensure_entity_coverage existe e é chamada no EnhancedSearch quando 2+ tickers são detectados.
11. Confirme que detect_query_intent retorna pelo menos os tipos: comparative, temporal, ranking, numeric, conceptual.
12. Confirme que o vector_store.search aceita e utiliza o parâmetro query_intent para ativar o ORDER BY híbrido temporal.
13. Confirme que o CompositeScorer aplica os boosts corretos: +0.15 para blocos table/key_metrics em queries numeric, +0.08 para tópicos dividendos/performance.

---

### BLOCO 4 — SYSTEM PROMPT DO STEVAN

14. Verifique se o system prompt do Stevan contém a instrução negativa sobre dados numéricos (nunca citar DY, P/VP, vacância, taxas que não estejam literalmente no texto recuperado).
15. Confirme que o comportamento de fallback está descrito: "Não encontrei esse dado nos documentos indexados" + oferta de busca na internet ou ticket.
16. Verifique se houve alguma alteração no system prompt desde o último deploy que não foi revisada intencionalmente.

---

### BLOCO 5 — FRONTEND / SIDEBAR DE OBSERVABILIDADE

17. Confirme que a sidebar do agent_test exibe: Tipo de Query, Entidades detectadas, Score colorido por documento (verde ≥0.70, laranja ≥0.50, vermelho abaixo), Badges de tipo de bloco.
18. Verifique se há algum import, componente ou rota no frontend que referencia um arquivo que foi renomeado ou deletado recentemente.
19. Confirme que o servidor de frontend (Vite, Flask serve, ou equivalente) está configurado corretamente no .replit / replit.nix para o ambiente de produção.

---

### BLOCO 6 — DEPENDÊNCIAS E AMBIENTE

20. Verifique se o requirements.txt (ou pyproject.toml / package.json) está atualizado com todas as dependências usadas no código atual. Identifique qualquer import no código que não aparece no arquivo de dependências.
21. Confirme que o comando de run no .replit está correto e aponta para o entry point certo.
22. Verifique se há algum arquivo .env no repositório (isso é ⚠️ ATENÇÃO — deve usar Secrets do Replit, não .env).

---

### BLOCO 7 — SMOKE TEST RÁPIDO (executar antes do deploy)

Execute os seguintes testes mínimos e reporte o resultado:

a) Importar EnhancedSearch e instanciar sem erros.
b) Chamar detect_query_intent("compare MANA11 vs LIFE11") e confirmar que retorna "comparative".
c) Chamar detect_query_intent("DY do último mês") e confirmar que retorna "temporal" ou "numeric".
d) Fazer uma busca de teste com query="vacância MANA11" e confirmar que o pipeline retorna ao menos 1 resultado sem exceção.
e) Confirmar que RetrievalLog consegue ser instanciado e que os campos novos (intent_detected, etc.) existem no modelo.

Se qualquer um desses falhar, é ❌ BLOQUEANTE.

---

### VEREDICTO FINAL

Com base nos blocos acima:
- Liste todos os itens ❌ BLOQUEANTE encontrados.
- Liste todos os itens ⚠️ ATENÇÃO que precisam de decisão consciente antes do deploy.
- Emita: **APROVADO PARA DEPLOY** ou **REPROVADO — resolver antes de prosseguir**.